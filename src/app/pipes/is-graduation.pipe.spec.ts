import { IsGraduationPipe } from './is-graduation.pipe';

describe('IsGraduationPipe', () => {
  it('create an instance', () => {
    const pipe = new IsGraduationPipe();
    expect(pipe).toBeTruthy();
  });
});
